{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Organize 桌面壳默认权限：窗口 + 系统通知 + 全局快捷键 + 自动更新/重启 + deep link。remote.urls 与 tauri.conf.json frontendDist 同步维护（ADR 0004 已知坑 2）：dev=localhost:3000（tauri dev），prod=生产域名（当前仍为占位域名 organize-web.vercel.app，被无关第三方占用；上线时与 frontendDist 一起切换，步骤见 docs/deploy-runbook.md §6.4）",
  "windows": ["main", "notch-trigger-*", "notch-panel"],
  "remote": {
    "urls": [
      "http://localhost:3000",
      "https://organize-web.vercel.app"
    ]
  },
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-emit",
    "notification:default",
    "global-shortcut:default",
    "updater:default",
    "process:allow-restart",
    "deep-link:default"
  ]
}
