# Organize 总路线图与 Agent 任务包

> 状态：当前唯一有效的项目路线图
>
> 基线：`master` at `edcb3dc`（PR #19，2026-07-29）
>
> 更新原则：每个 PR 合并后同步状态；历史方案只保留背景，不再复制待办。

## 1. 产品方向

Organize 的核心不是堆出另一个全能知识库，而是打通一条稳定闭环：

`收集网页 -> 阅读与高亮 -> 写笔记/任务/经验 -> 在工作台回顾 -> 可恢复地沉淀`

短期优先级依次是：

1. 数据不丢、状态不乱、错误可恢复。
2. 高频编辑与回顾流程足够顺手。
3. 再扩展离线、桌面、移动端和 AI。

## 2. 当前能力盘点

| 领域 | 当前状态 | 说明 |
| --- | --- | --- |
| 工作台 | 已完成 | “今天 / 回顾 / 统计”已在 PR #18 合并到一个工作台，以页内标签切换；旧 `/review`、`/stats` 仅保留兼容入口。 |
| 阅读 | 已完成基础闭环 | 收集、抓取、三态阅读进度、高亮、标签、批量操作和生命周期管理已存在。 |
| 笔记页面 | 已完成核心能力 | 标题拆分、长标题、图标、封面、评论、分享、收藏、子页面与侧栏树已实现。 |
| 块编辑器 | 持续打磨 | 已有列表样式、折叠、任务、引用、代码、公式、标注、列布局、块选择/拖动；PR #19 已补齐表格选择器和表格工具栏。 |
| 数据安全 | 已完成第一阶段 | 公开分享加固、备份恢复、软删除与垃圾箱已落到迁移 018–024。 |
| 插件 | 可用但测试不足 | 内置插件可注册/启用，配置通过 API 持久化；缺少系统级回归与错误反馈。 |
| 离线 | 仅有骨架 | Service Worker 有页面缓存，IndexedDB 有队列原语，但没有真正的同步执行、重试和冲突处理。 |
| 桌面/移动 | 骨架 | Tauri 与 Capacitor 尚未形成可发布产品。 |

## 3. 路线图

### P0：可靠性与数据一致性

- [ ] **R1 页面展示偏好入库**：把字体、小字号、全宽从单机 `localStorage` 迁移为按用户隔离的笔记字段，并纳入备份恢复。
- [ ] **R2 插件配置回归与错误反馈**：覆盖初始化、启停、配置保存失败，避免静默失效。
- [ ] **R3 离线能力重新定界**：在真正同步完成前，界面不得把页面缓存描述成“支持离线编辑”。
- [ ] **R4 关键写入可观察性**：统一自动保存、分享、备份恢复和批量操作的失败提示与重试入口。

### P1：高频效率

- [ ] **E1 页面更多菜单搜索**：支持过滤、上下键、Enter 和 Escape，命令只有一个实现入口。
- [ ] **E2 富文本复制**：拷贝页面内容同时写入 `text/html` 与 `text/plain`，失败时降级。
- [ ] **E3 页面移动体验**：复用现有 `parent_note_id` 和防循环逻辑，补齐从列表/页面菜单移动到父页面的流程。
- [ ] **E4 笔记链接体验**：补齐反向链接、失效链接反馈和子页面路径导航。
- [ ] **E5 编辑器回归矩阵**：把列表、列、表格、拖动和多选的组合场景变成稳定自动化测试。
- [ ] **E6 笔记冲突与本地草稿**：content_revision 乐观锁已在 G1(#48)落地，需补前端冲突 UI（他人改了同一篇笔记时的提示/合并/本地草稿）。
- [ ] **E7 块搜索**：在笔记内按块粒度搜索定位（区别于现有页面级搜索）。
- [ ] **E8 恢复 UI 与附件**：垃圾箱恢复入口、附件管理面板。
- [ ] **E9 循环 / 子任务 / 依赖**：任务循环检测、子任务层级、任务间依赖关系（G0–G4 双链已建引用模型，可复用）。
- [ ] **E10 阅读 → 笔记 → 任务链路**：reading_items 高亮 → 笔记 → 任务的贯通引用。

### P2：平台能力

- [ ] **X1 离线同步**：定义幂等协议、顺序、重试、冲突提示和可恢复状态后再实现。（注：`lib/offline/queue.ts` 当前是 0 引用死代码，需先清理或重新定界。）
- [ ] **X2 桌面端最小发布版**：登录、收集、阅读、笔记、自动更新和安全存储形成闭环。（注：`desktop/` Tauri 配置指向不存在的 `apps/web/out`，需先开 `output:'export'`。）
- [ ] **X3 移动端最小发布版**：分享接收、离线阅读、快速记录和同步状态形成闭环。（注：`mobile/` 仅有 capacitor.config.ts 占位，无原生工程。）
- [ ] **X4 AI 边界**：先补数据发送提示、作用范围、限流和失败回滚，再增加更多 AI 动作。
- [ ] **X5 知识图谱**：基于笔记间链接 / 任务依赖的可视化图谱（依赖 E4 反链、E9 依赖先落地）。
- [ ] **X6 多人协作**：实时协作编辑（依赖 G3 Realtime 基础设施 + 权限模型）。
- [ ] **X7 语义 AI**：语义搜索 / 自动关联（依赖 X4 AI 边界）。

## 4. 对抗式分工结论

**结论：可分工，但只能按耦合和可验收性分，不能按代码行数分。**

最可能失败的三点：

1. 两个 Agent 同时修改 `notes/[id]/page.tsx`、`tiptap-editor.tsx` 或迁移，产生语义冲突。
   门禁：同一时间这些高冲突文件只归一个任务包，其他 Agent 禁止触碰。
2. 普通 Agent 把“按钮出现”当作“功能完成”。
   门禁：每包都要求失败路径、刷新后状态和真实浏览器验收，不接受仅截图或仅类型检查。
3. 委派成本超过实现成本。
   门禁：少于半天且需要大量上下文重建的任务留给当前主 Agent；普通 Agent 只接文件边界清楚、验收可机械判断的包。

## 5. 可直接复制给其他 Agent 的任务包

### A1 页面更多菜单搜索

**等级：普通 Agent 可独立完成**

```text
目标：给笔记详情页“更多”菜单增加命令搜索，支持输入过滤、ArrowUp/ArrowDown、
Enter 执行和 Escape 关闭。命令行为必须复用现有回调，不复制保存/删除逻辑。

开始前：git pull origin master；阅读 AGENTS.md 和
docs/organize-roadmap-and-agent-tasks.md；若目标文件有未提交改动立即停止。

允许修改：
- apps/web/components/notes/note-page-menu.tsx
- 与该组件同目录的新测试文件

禁止修改：
- supabase/migrations/**
- apps/web/app/(main)/notes/[id]/page.tsx
- apps/web/components/editor/**

验收：
- 空查询显示原分组顺序。
- 查询无结果有空状态。
- 键盘上下选择不越界，Enter 只执行一次。
- 开关、危险操作和禁用项的原行为不变。
- pnpm --filter @organize/web test
- pnpm --filter @organize/web typecheck
- git diff --check
```

### A2 富文本复制页面内容

**等级：普通 Agent 可独立完成，完成后需审查**

```text
目标：把“拷贝页面内容”升级为同时写入 text/html 和 text/plain；
ClipboardItem 不可用或权限失败时降级到纯文本，并显示现有 toast。

允许修改：
- apps/web/components/notes/note-page-menu.tsx
- apps/web/lib/export/ 下的新转换辅助文件
- 对应测试文件

禁止修改：
- 编辑器 schema 与扩展
- 数据库迁移和 API

验收：
- 标题、段落、列表、引用、代码和表格有合理 HTML。
- 不把脚本、事件属性或未转义标题写入剪贴板。
- 不支持 ClipboardItem 时仍能复制纯文本。
- 空正文和剪贴板拒绝权限都有明确结果。
- 全量 test、typecheck、build、git diff --check 通过。
```

### A3 插件配置回归测试与错误提示

**等级：普通 Agent 可独立完成，完成后需审查**

```text
目标：为 PluginLoader 的配置读取/保存补回归测试，并将初始化或保存失败从
console.log/静默跳过改为用户可见的非阻塞提示；不要改变插件 SDK。

允许修改：
- apps/web/components/plugin/**
- apps/web/lib/plugin/**
- apps/web/app/api/plugins/** 的测试文件

禁止修改：
- packages/plugin-sdk/**
- supabase/migrations/**
- 内置插件业务逻辑

验收：
- 覆盖已有记录、首次创建、disabled、GET 失败、PATCH 失败。
- 相同插件不会重复注册或重复创建记录。
- 错误提示不包含密钥或原始数据库错误。
- 全量 test、typecheck、build、git diff --check 通过。
```

### B1 页面展示偏好入库

**等级：完成后必须重点审查**

```text
目标：把 fullWidth/font/smallFont 从 localStorage 迁移到 notes 页面设置字段，
旧笔记保持默认值，登录后跨设备一致，并纳入备份/恢复。

必须先提交设计说明，不得直接写迁移。设计需覆盖：
- 字段与默认值
- RLS/GRANT
- API 白名单
- mock 数据
- 备份 schema、导出、恢复和旧备份兼容
- localStorage 一次性迁移与失败回退

主要边界：
- supabase/migrations/ 新增一个迁移，不修改历史迁移
- notes API、共享类型、页面和 backup 全链路

验收：
- 两个用户不能互读/互改设置。
- 旧记录、旧备份、无 localStorage、保存失败均有测试。
- Supabase 本地迁移、全量 test/typecheck/build、真实刷新回归通过。
```

### B2 页面移动与层级体验

**等级：完成后必须重点审查**

```text
目标：在笔记更多菜单和笔记列表提供“移动到”，复用现有 parent_note_id、
树构建和循环防护；移动后侧栏、面包屑和子页面列表立即一致。

禁止另建文件夹模型，禁止绕过现有 notes API 直接拼接客户端树。

验收：
- 不能移动到自己或自己的后代。
- 根页面、深层页面、被删父页面和并发更新有明确结果。
- 移动失败时 UI 回滚，不出现侧栏与详情页不同步。
- 树单测、API 测试、真实浏览器移动/刷新/撤销检查通过。
```

### C1 离线同步

**等级：必须由强 Agent 实现并重点审查**

不要直接把 `lib/offline/queue.ts` 接到所有写入。先设计操作幂等键、依赖顺序、
认证过期、部分成功、重试上限、冲突展示和队列迁移；设计评审通过后才能编码。

### C2 桌面端 / 移动端发布

**等级：必须由强 Agent 规划，普通 Agent 只接机械子任务**

主 Agent 负责认证、安全存储、同步协议、升级与发布链路。普通 Agent 可以在接口
冻结后承接图标、静态文案、构建脚本检查和既定用例补齐。

## 6. 并行与审查规则

- 一次只允许一个 Agent 修改编辑器核心、笔记详情页或数据库迁移。
- 每个 Agent 使用独立 worktree 和 `codex/`、`feat/`、`fix/` 等特性分支。
- 接任务时复制完整任务包，不要只发一句“继续完善”。
- 审查必须同时读取任务包和 diff；只看 diff 会漏掉未实现的需求。
- 普通 Agent 的 PR 不自动合并。先过 CI，再由主 Agent 按验收项复测。
- 表格/列表/拖动等编辑器改动必须验证旧 TipTap JSON，可见 DOM 正常不等于持久化正确。

## 7. 文档治理

- 本文是路线图与分工的唯一事实来源。
- `docs/note-page-next-agent-plan.md` 是历史设计资料。
- `.zcode/plans/plan-sess_619a7b86-2d27-4ab7-8df0-e8b20e1d3312.md`
  是早期计划快照，内容大多已实现；确认无外部依赖后可单独删除。
- 每次路线图更新都要以代码、迁移、测试和真实页面为证据，不能只改勾选框。
