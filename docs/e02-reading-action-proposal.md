# E02 阅读→行动闭环——现状盘点与单一路径提案（E02-1）

编制：2026-09-16。决策依据：账本「用户决策记录（2026-09-15）」B4——仅 E02 立项，先做单一路径提案。
代码基线：master `5d51c2e`。

## 1. 结论先行

**E02 卡面的核心验收路径已由既有实现完整满足**（042 迁移时代的「高亮引用关联」体系），无需从零开发。
本提案完成卡面要求的「现状→目标映射」与逐条验收对照，并识别出唯一值得做的体验增强
（任务→来源的**高亮锚点定位**回跳），作为可选子卡 E02-2 供后续领取。

## 2. 现状映射（既有能力，file:line 证据）

| 能力 | 实现 |
|---|---|
| 高亮→转为任务/笔记（显式按钮） | `components/reading/highlights-panel.tsx:81-94`（「转为任务」「转为笔记」，仅在该侧无引用时显示） |
| 幂等转换（不生成重复） | RPC `convert_highlight_reference`（迁移 042）：重复转换返回 `status="existing"`，UI 提示「已存在」且不再新建（`app/(main)/library/[id]/page.tsx:682-711`）；高亮行上 `task_id`/`note_id` 唯一挂载 |
| 来源回跳（高亮→行动物） | 引用 active 时按钮变「打开任务/笔记」（highlights-panel.tsx:96-108） |
| 来源回跳（行动物→阅读） | 任务工作台 `TaskLinkedContent`（`components/tasks/task-linked-content.tsx:93-99`）渲染「关联阅读/笔记」链接，指向 `library/[id]` / `notes/[id]` |
| 源删除清楚状态 | 引用三态 `active/deleted/missing`（`lib/reading/highlight-references.ts`）+ `get_linked_content_states` RPC（042/065）：双侧呈现「已在垃圾箱」「引用已失效」（highlights-panel.tsx:110-114、task-linked-content.tsx:82-87） |
| 不替用户自动决定 | 转换全部由显式按钮触发；高亮创建时可选附带转换（`handleCreateHighlight` 的 `targetType` 参数），无任何自动建任务/复盘行为 |
| 高亮→原文位置（页内） | 面板点击高亮滚动到正文对应 `mark` 并高亮 1.5s（highlights-panel.tsx:51-63） |

## 3. 卡面验收逐条对照

| E02 验收项 | 对照结论 |
|---|---|
| 不生成重复任务/笔记 | ✅ RPC 幂等（existing 分支） |
| 来源回跳 | ✅ 双向（行动物↔来源均有一键跳转与状态呈现） |
| 源删除后有清楚状态 | ✅ 三态语义 + 双侧「垃圾箱/失效」文案 |
| 不替用户自动决定任务与复盘关系 | ✅ 全显式触发 |

## 4. 识别的缺口（唯一值得做的增强）

> **交付更新（2026-09-16，E02-2 已实现）**：实现时核实 §4 原前提有误——014 的
> `anchor_path`/`anchor_offset` 在创建高亮时从未写入（insert 仅
> user_id/reading_item_id/content/color），存量高亮无锚点可用。实际落地改为
> 空白归一化文本匹配定位（`lib/reading/highlight-locate.ts`），任务侧链接带
> `?hl={highlightId}`、阅读页定位 mark 并滚动闪烁、找不到静默降级；证据见
> 账本 E02 行。以下原文保留。

**任务→来源高亮位置无锚点定位**：任务侧「关联阅读」跳到 `library/[id]` 条目顶部；
高亮在正文中的位置需要用户自行寻找（页内滚动定位仅在高亮面板内部可用，
且依赖 `mark.textContent === highlight.content` 的全文匹配，重复文本会命中第一个）。

**提议子卡 E02-2（可选，S 规模）**：跨页高亮锚点回跳——
`TaskLinkedContent` 的阅读链接携带来源高亮标识（`library/[id]?hl={highlightId}`），
阅读页加载后按 `anchor_path`/`anchor_offset`（014 已存储的锚点数据）定位并滚动到对应 mark，
找不到时静默降级为现状（条目顶部）。不新建任何数据，纯前端定位增强。

## 5. 建议与后续

1. **E02 主路径按「已存在、验收满足」收口**（本提案即卡面要求的「单一路径提案」交付）。
2. E02-2（锚点回跳增强）为可选增强，是否领取由后续按使用反馈决定；不领不影响 E02 收口。
3. 若用户实际使用中发现其他「仍需重复复制」的路径（如阅读→速记、摘录→周报），
   按账本流程追加候选，不在本提案范围。

## 6. 本提案不包含

不修改任何业务代码；不新建迁移；不动 UI。纯文档交付（决策 B4「先做单一路径提案」）。
