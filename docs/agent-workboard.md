# Organize Agent Workboard（任务卡唯一状态账本）

> 本文件是任务卡的**唯一状态真相源**，由 [长期计划](long-term-agent-plan-2026-09-11.md) A01 建立（2026-09-11）。
> 规则：每张卡完成后只更新本文件的对应行；旧 `docs/ROADMAP.md` / `PROGRESS.md` / `BLOCKED.md` / `docs/handoff/execution-log.md` 保留为历史正文，顶部已链接到这里，不再作为当前状态入口。
> 状态取值：候选、就绪、进行中、待验证、阻塞、完成、取消。代码已写但真机/真实后端未测，必须标「待验证」。
> 卡片定义（范围/验收/边界）以计划原文为准，本表只维护状态与证据，不复制卡面。

## 全局基线（2026-09-14，C02 第五轮 + C05 S1 合并时更新）

- master = `9e6e062`（C05-S1 后）；迁移 `001–079`；备份 `BACKUP_VERSION = 5`（`apps/web/lib/backup/schema.ts:2`）。
- Vitest 基线：A03 后 148 文件 / 1,099 用例；A04 后 148 / 1,101；A05 后 148 / 1,108；A06 后 web 148 / 1,114 + collab-server 24；B01 后 web 149 / 1,128；B02 后 web 150 / 1,135（+7 probe 单测，本地实跑全绿）；B03 后 web 150 / 1,140（+5 反链读路径单测）；B04 后 web 151 / 1,147；B05 后 web 153 / 1,163（+11 播种状态机 +5 来源解析）；C02-1 无单测增减（E2E 回归 +1：a11y-button-names 进 e2e-test job）；C05-S1 后 web 155 / 1,172（+10：点击导航 jsdom 4 + 重试决策 5 + taskId 1），E2E 回归用例 2→3。C02-6 后 web 156 / 1,193（+21：品牌安全文本 token 对比度契约）；C01 键位校验后 web 157 / 1,201（+8）；B07-2 后 web 158 / 1,211（+10 附件包导出，新增依赖 `fflate`）；C05-S2 后 web 158 / 1,217（+6 双响收敛）；C05-S3 后 pgTAP 34 文件 / 911 断言（080 +17：墙钟推进）；C05-S4 本地段后 web 159 / 1,223（+6 cron 路由投递分支）。以各 PR CI 复核为准。
- 验证门禁：web typecheck / Vitest / 零警告 lint / build；UI 改动跑相关 E2E；数据库改动跑隔离 pgTAP（含越权负例）；协作改动跑 collab-server build/test + 真实协作 E2E。纯文档卡只做链接/内容检查。
- 遗留开放 PR：#210（Chrome 扩展）、#212（plugin 包类型检查修复）——均早于本计划（2026-09-01/02 创建），是否收编或关闭**待用户决定**，接力 Agent 不得擅自合并或关闭。

## 卡片状态

### A 波次：事实基线与发布/协作风险证据

| ID | 卡 | 规模 | 状态 | 依赖 | 执行者 | PR/commit | 关键证据 | 未验证项 |
|---|---|---|---|---|---|---|---|---|
| A01 | 唯一状态账本 | S | 完成 | — | engineering-agent | #260 | 本文件；旧文档顶部链接已加 | — |
| A02 | Service Worker 跨版本更新与离线边界 | M | 完成 | A01 | engineering-agent | #261 | 复现证据（2026-09-11，/tmp/sw-repro）：未缓存脚本离线收到 `200 text/html`（sw.js 旧实现回退 `/`）；断网刷新 hydration 失败；sw.js 字节不变致 `update()` 无 waiting；旧构建 chunk 在新构建服务 404。修复：版本化缓存（gen-sw 构建注入时间戳）、类型分流回退（仅导航可回退 HTML，最终到零依赖 `public/offline.html`）、用户确认式安全激活（SKIP_WAITING 消息 + 非阻塞「新版本已就绪」提示，不强制刷新）、保留上一版缓存供旧标签页、`app/error.tsx` 旧 chunk 失败兜底文案。行为证据：`e2e/sw-update.spec.ts` 双构建 6 条全过（本地 SW_E2E=1 实跑）；CI 新增 sw-e2e job 常跑。**A04 追加修复（同缺口实测）**：middleware 307 拦截 `/sw.js` 致未认证页注册永久 SecurityError，且登录为 SPA 导航 registrar 不重试 → 登录会话全程无 SW；已豁免 `/sw.js`/`/offline.html` + registrar 有界重试（见 A04 PR） | 真实生产灰度观察；真实后端双账号切换实机验证；**SW 缓存页离线重开仍不可读笔记（X1-2B 合同，本地草稿读取未接通离线失败路径，A04 实测确认）** |
| A03 | 真实后端和协作 CI | M | 完成 | A01 | engineering-agent | #262 | 修复前证据：ci.yml 无 collab build/test、协作 E2E 依赖 `COLLAB_E2E=1` 从不进 CI、Supabase CLI `version: latest`（漂移）。修复：verify job 加 collab-server build/test；新增 collab-e2e job（完整本地 Supabase + seed 双账号/匿名分享 + collab-server:1420 + 真实后端 web:3100，显式 `COLLAB_E2E=1`，服务启动失败即失败，诊断工件上传，栈密钥不回显）；Supabase CLI 固定 2.116.0（db-test 同步）；master 分支保护补 required checks。验收：PR #262 CI 五 job 全绿（verify/e2e-test/sw-e2e/collab-e2e/db-test 均 SUCCESS，2026-09-12 复核）；登录双账号/匿名协作/刷新持久化由 collab.spec + anon-collab.spec 实跑覆盖 | — |
| A04 | 同步块双浏览器可靠性验收 | M | 完成 | A03 | engineering-agent | #263 | 交付：`e2e/synced-block.spec.ts` 7 场景（旧 hydrated 不信任/两页同步/并发分叉/断网重开/响应丢失 409 幂等/配额兜底/撤权等价态）+ `seed-synced-block-e2e.mjs`（幂等 + 清 note_ydocs 残留）+ 接入 CI collab-e2e job（`REAL_DB_E2E=1`）。**实跑发现并修复 4 个真实缺陷**：① 登录路径协作播种从未工作——上游 `@tiptap/extension-unique-id@2.27.2` onCreate 在 provider synced 时给空文档 dispatch 补 id，更新先于 seed-req 到达 → 服务端 markSeeded → 租约 deny → 编辑器停空文档（collab/anon 因不依赖预播种内容/另一套编辑器而未暴露）；协作模式改为抑制该 onCreate，初始回填由手动 effect 接管。② 空文档反向覆盖丢数据——回填等待以 `seedContent` 非空为前提（页面异步加载，可能晚于 synced），且 8s 封顶后给空文档补 id 并 `onUpdate('hydrate')` → 保存链把空文档写回 notes.content（实测种子笔记 3 块被清空、标题丢失）；改为无条件等待 + 封顶仍空不写不存。③ 同步块 409 幂等比较键序敏感——服务端 jsonb 规范化键序 ≠ 客户端插入序，`JSON.stringify` 恒不等 → 重试永远降级 conflict；改 `stableStringify`（递归键排序）比较。④ middleware 307 拦截 `/sw.js`——SW 脚本请求不允许重定向，未认证页注册 SecurityError + 登录 SPA 导航不重试 → 登录会话全程无 SW；豁免 `/sw.js`/`/offline.html` + registrar 有界重试。**测试基建**：三个种子脚本删固定 UUID 笔记的 note_ydocs（重跑同一起点）；collab.spec openNote 等播种完成（≥2 块）。**证据**：本地完整栈（真实 Supabase + collab-server + 真实后端 web 构建）全套 10/10 连续两轮全绿（可重复性）；Vitest 148 文件/1,101 用例；typecheck/lint 零告警 | CI collab-e2e 首跑本 spec（本地两轮绿，CI 时序可能暴露新 flake）；发现「协作会话建立前输入随编辑器重建丢失」窗口（collab.spec 曾因此时序失败，已加等播种守卫，产品修复归 A05）；「SW 缓存页离线重开不可读」记入 A02 未验证项 |
| A05 | 协作会话刷新与撤权（先设计） | L | 完成 | A03 | engineering-agent | A05-1 #265；A05-2 #266；A05-3 #267；A05-4 #268 | **上游核实（2026-09-12，推翻 BLOCKED P5-03 旧结论）**：`@hocuspocus/provider@4.6.0` 的 `token` 配置接受函数，每次 WS onOpen（含重连）与服务端 TokenSync 请求时重新求值；server 有 `connection.requestToken()` + `onTokenSync` hook 可对存量连接重验权限（抛错即 close），`connection.readOnly` 每消息检查（editor→viewer 降级无需重连）。设计定稿：`docs/collab-session-refresh-design.md`（缺陷 8 项/状态机/撤权窗口 ≤5min+17s）。**A05-2 已交付（#266 客户端）**：token 函数；authenticationFailed 3 次退避重握手耗尽降级；GATE_TIMEOUT=10s 门控（计时从会话建立起点算——实测 auth.getUser 偶发 15s+ 挂起，原方案会挂死成永久只读；已删冗余 getUser 改用 session.user.id）；SIGNED_OUT/同页切号销毁重建；页面 collabResolved 门控（编辑+草稿对话框）；编辑器播种锁；seed-deny 12s toast；分享编辑器 error 降级。Vitest 148 文件/1,107 用例；本地完整栈协作 E2E 10/10 两轮。**A05-3 已交付（本 PR 服务端）**：validateAccess 共享判定链（onAuthenticate/onTokenSync 同一条，不自建第二套权限）；onTokenSync + decideReauth 纯策略（reauth.ts）——无权/身份漂移/token 形态漂移 → close，有权 → **就地**更新 context（token/role，返回新对象会让 hookPayload.context 换引用而 connection.context 留旧值，写者凭证链会断）+ connection.readOnly 同步（editor↔viewer 免重连）；connected 起每 REAUTH_INTERVAL_MS（默认 5min，COLLAB_REAUTH_INTERVAL_MS 可覆盖）requestToken 定时器，onDisconnect 回收；修 D3 长会话 JWT 过期 blob 持久化静默失败。单测 10 条新增（全套 14 过）；本地完整栈 E2E 10/10 回归；**3s 间隔真实撤权冒烟**：psql 删 workspace_members → server `[reauth] close` → 客户端「鉴权失败，退避重握手 attempt 1」按设计联动，编辑全程不丢 **A05-4 已交付（本 PR E2E）**：`e2e/collab-revocation.spec.ts` 三场景（① editor→viewer 降级免重连：撤权后 B 输入不再到达属主、连接不断页面不降级；② 移除访问：重验 close → 客户端退避耗尽降级提示、页面存活；③ 公开链接关闭 access_mode=disabled：匿名连接 close → 分享页只读快照提示、内容保留）进 CI collab-e2e job（server 注入 COLLAB_REAUTH_INTERVAL_MS=3000）。**排查中修复的三个真实客户端缺陷**：(a) 服务端文档级 CLOSE 消息不关 socket、provider 只清状态不重鉴权 → 客户端补 close 事件主动重握手（独立退避 1s×2^n 上限 10s，synced 复位）；(b) 重试链依赖「第 4 次失败回报」会因 WS 层退避挂起 → 末次重试加鉴权看门狗（+10s 未重新 authenticated 即降级，authenticated 清计数）；(c) 测试输入落点竞态两类——随机 base36 后缀偶含 "gr" 触发全局 G-R 跳转（编辑器只读时按键逃逸到 body，热键守卫本身正确）→ 测试点击后验证焦点进编辑器；分享页协作绑定重建编辑器实例吞输入 → 等待策略改为定形后验证。**另修种子**：seed-collab-e2e 写入 userAId/userBId（admin API ?email= 过滤在本地 GoTrue 不生效，曾致撤错人）。**验证**：本地完整栈 13/13 连续两轮（10 旧 + 3 新）；Vitest 148 文件/1,108 用例；typecheck/lint 零警告 | 真实生产环境未验证（D01 领卡时复核——本地完整栈与 CI 已覆盖验收语义） |
| A06 | 匿名入口多实例限流 | M | 完成 | A05、扩大公开部署前 | engineering-agent | 本 PR | 设计先行：`docs/anon-rate-limit-design.md`（拓扑/威胁 T1–T5/机制选型/故障策略）——共享计数选 Supabase Postgres（不引入付费服务，卡面依赖约束），**默认 memory 保留现状**，多实例时显式 `RATE_LIMIT_BACKEND=postgres`（web/collab 同名变量）。交付：迁移 076（`rate_limit_hits` 表 RLS 无 policy + 表级权限收口 + `consume_rate_limit` 固定窗口 UPSERT 原子自增 + `purge_rate_limit_hits` 概率清理；窗口时钟取 DB clock_timestamp 防实例漂移；purge 只授 service_role——**实测本地栈会给新函数自动补 anon EXECUTE，迁移末尾显式 revoke 防御**）；web `lib/api/rate-limit.ts` 加 `checkRateLimit` 双 backend（RPC 失败不重试回退进程内档，warn 不打 key）+ save/invite 两路由接入；collab-server 握手限流抽为 `src/anon-auth-limiter.ts`（同级键语义不变，rpc 注入可测）。**验证**：pgTAP 076 43 断言（多实例合计/RLS 负例/窗口滚动/参数校验/purge）全套 30 文件 839 断言 PASS；web Vitest 148 文件/1,114 用例（+6）；collab-server 24/24（+10）；`scripts/verify-shared-rate-limit.mjs` 两实例实测 3/3（web :3101+:3102 与 collab :1421+:1422 均 postgres：token+IP 档合计第 31 次 429、轮换伪造 XFF 总量档第 121 次 429、WS 握手合计第 121 次拒——**修出真缺陷：key 正则漏点分 `.` 会让 IP 档恒 fallback，已修并用 pgTAP 钉住**）；memory 默认路径 E2E 13/13 回归零退化；typecheck/lint 零警告。CI collab-e2e job 追加双实例验证步骤（主 3100/1420 保持 memory 不动 13 spec 回归路径）；deploy-runbook 加多实例必配清单；BLOCKED.md 声明 3 改写为双 backend 合同 | 真实多实例生产拓扑未部署（D01 阻塞中；本地/CI 双实例已覆盖验收语义）；固定窗口边界 2× 突刺为记录在案的已知边界 |

### B 波次：数据恢复、性能、关系完整性、可维护

| ID | 卡 | 规模 | 状态 | 依赖 | 执行者 | PR/commit | 关键证据 | 未验证项 |
|---|---|---|---|---|---|---|---|---|
| B01 | 备份 v5 完整恢复演练 | M | 完成 | A01 | engineering-agent | 本 PR | **真实后端全链路演练**：`apps/web/scripts/backup-restore-drill.mts`（tsx 直跑）——admin 建每轮专用 A/B 账号 → service_role 播种 57 行（29 表全覆盖：复杂正文含内链/taskItem 绑定/同步块/数据库块、任务层级+依赖+清单+提醒+附件+活动、task_item_refs 双链、memo_notes、高亮三类引用、三态阅读、页面设置、软删速记）→ **A 会话走生产导出代码**（fetchBackupData+createBackupV2，RLS 真实可见性）→ **B 空账号走生产恢复代码**（inspect→prepareRestorePayload 同序 uuid 队列捕获映射→restore RPC）→ 读回 B 逐表逐行比对（70 断言，三轮全绿）；负例：非空账号 not_empty、截断/非法 JSON、>10MiB 拒绝；软删除语义：回收站行不进导出、回收站速记带 deleted_at 无损往返、last_edit_by 不搬运。**演练修出五缺陷（RED ba58cd8→GREEN 全有证据）**：① 回收站行子行（note_versions/note_tags/task_checklists/dependencies/reminders/attachments/activities/task_item_refs——RLS 不过滤父行软删）泄漏→BROKEN_REFERENCE→**有回收站内容即整份导出失败**（pruneExportData 剪枝：孤儿行剔除+悬空可选引用置 null+父缺席子任务升级根任务）；② 内容级悬空内链（指向回收站笔记）炸 inspect/restore（放宽为合法产品态：inspect 删内容级检查、restore 悬空 href/syncedId/databaseId/taskId 原样保留由 043 失效装饰呈现，表级仍严格）；③ **tasks.list_id 从未进恢复链+导出缺列→往返后全部任务脱列**（导出列+schema 模板+迁移 077 后置 UPDATE 补写，悬空 list_id FK 整体失败）；④ task_lists 软删行导出后复活为活跃列表（导出 activeOnly 过滤）；⑤ **真实 v2/v3/v4 文件不可导入**（075 只补 v4 counts 兼容漏 data 补空、v2/v3 两处清单漏 memo_notes→INVALID_TABLE；v2/v3 补全部新表、v4 补 memo_notes）。settings 页导出接入共享模块（与演练同源）。**验证**：pgTAP 31 文件/848 断言（077 九断言：list_id 落库/旧文件 null/FK fail-closed 无部分写入/040 预检不变）；Vitest 149 文件/1,128 用例（export-drill.test.ts 14 条：剪枝/悬空/list_id/v2/v4/损坏/超限）；typecheck/lint 零警告 | 真机 UI 上传路径（restore-section→/api/backup/restore）未真机点按：route 层为薄壳（inspect/prepare/RPC 与演练同一代码），mock 冒烟覆盖入口；Storage 附件文件本体排除如实可见沿用 P0-04 声明 |
| B02 | 完整性能测量 | M | 完成 | A01 | engineering-agent | 本 PR | 报告 `docs/handoff/b02-measurement.md`。**仪表修复（R12 两缺口）**：新 `lib/perf/probe.ts` 应用层直报（不再包装 fetch——R12 坑：supabase-js 模块加载捕获 window.fetch 引用，事后包装无效）：savePosts/saveFailures 由 note-save-session.runSaveRound 上报、draftSize（last/max）与序列化耗时由 persistDraft→writeLocalNoteDraft 上传（结果附加 bytes/durationMs 字段）；longtask/INP(event timing) 观察器随 PluginBootstrap 启用；人工抽查 `__organizePerf.snapshot()` 与驱动同源；probe 7 条单测。**测量**：`scripts/perf/measure.mts`（非 CI），M4/16GB/Chromium 1.62.1 生产构建 + 本地真实后端 + 协作关闭（R12 口径）；固定种子 0xb02f00d；每轮重播种同起点；3 轮中位数（范围）。矩阵 9 格（1k/10k/50k 字 × 0/10/30 图）+ 复杂表格20×6（colspan/背景）+ 列表1k/10k + 图谱60笔记×5标签。**结果**：打字零长任务（全配置）；冷开 173–262ms、重开 192–233ms；5 万字保存 88–140ms（离群 334）；草稿峰值 92.5KB、序列化 <1ms；**列表1万条冷开 71,588ms（71,396–73,445）——唯一不可用级瓶颈（TipTap 大列表渲染，重开仍 1.6s）**；图谱 60 笔记 886ms/35.6MB。**B06 判断**：localStorage 草稿（≤92.5KB）+ 序列化 <1ms + 零长任务，两轮独立测量（R12+B02）均无存储瓶颈证据 → B06 按卡面约定转取消（见 B06 行），实测瓶颈登记编辑器域候选。局限如实记录：INP 合成输入=0、图片为注入 1×1PNG、草稿大小为 UTF-16 码元口径 | 移动端真机（Safari+软键盘+真实键盘 INP）、真实远图解码、图谱千笔记规模曲线——需真机/真实环境，待验证 |
| B03 | 精确关系索引 R10b | L | 完成 | B01/B02 | engineering-agent | B03-1 #273；B03-2 #274；B03-3 #275；B03-4 本 PR | **B03-1 设计定稿**（`docs/note-relations-index-design.md`）；**B03-2 迁移 078**（note_links 派生表 + 提取核心 + 触发器 diff 维护 + v2 RPC；实测修出 JSON null≠键缺失判停缺陷与游标裂缝缺陷，pgTAP 34 断言钉住；触发器开销 536KB UPDATE +4.5ms）；**B03-3 迁移 079**（rebuild/reconcile 批处理 RPC 仅 service_role + 驱动脚本 + 基准脚本；本地栈回填 665 篇 + **对账连续两轮 mismatched=0（切读门槛达成）**；基准 v1 233.7ms vs v2 30.8ms 全量翻页中位数 **~7.6×**，v1 成本随内容总量线性、v2 索引恒定）。**B03-4 切读**：`lib/notes/backlinks.ts` 重构为三函数——fetchAllNoteBacklinksV1（074 原逻辑，标回退路径勿新增调用方）/ V2（078 游标读：键缺失或 JSON null 均判停、5000 上限守卫）/ 合并入口（v2 优先，RPC 报错 warn 后自动回退 v1——发布竞态守门窗口，D01 复核后清理收编）；行形状两代一致 UI 零改动。**验证**：单测 8 条（v1 原有 3 + v2 游标/判停/错误 3 + 合并入口 v2 直走/回退 2），Vitest 150 文件/1,140 用例；真实后端 E2E 冒烟 `e2e/note-backlinks.spec.ts`（seed-backlinks-e2e.mjs 固定 UUID 幂等 upsert、078 触发器随 upsert 建边；A 打开目标笔记 → 反链面板列出来源标题 → 点击跳转来源）本地实跑通过并接入 CI collab-e2e job（testMatch + seed 步骤）；typecheck/lint 零警告；本地全 collab 套件中 collab/anon-collab 两例失败经 master 状态复跑确认为存量环境问题（dev server 无关本卡），CI 生产栈为准 | v1 回退守门窗口待真实环境复核（D01）；候选决策 3 项待用户（阅读反链 UI / 图谱切读索引 / 锚点上下文摘要）；生产库回填属运维操作未执行 |
| B04 | 编辑器块交互拆分 | M | 完成 | A03 | engineering-agent | 本 PR | **纯移动拆分**（主文件 2,307 → 1,362 行，无逻辑改动）：① `bubble-toolbar.tsx`（728 行）——blockOptions 块类型表/色板/表情/Dropdown/MenuItem/BubbleToolbar/InsertMenu 整体迁出（props 驱动自包含，全文件仅 BubbleToolbar 被外部引用）；② `block-interaction.ts`（299 行）——交互类型（HoveredBlock/BlockDropTarget/BlockPointerDrag/OpenMenuState 族）+ 几何/命中测试/菜单定位纯函数（nodePosForElement/handleTopForBlock/handleLeftForBlock/blockElementAtTarget/pointIsOverRenderedText/menuPointBelowBlock/activeTableReferenceRect/shouldShowTextToolbar）+ **keydown 工厂**（createBlockKeydownHandlers：IME 守卫/标题行尾回车/⌘/ 顶层块菜单/⌘F 搜索，行为逐行保持，从内联提为可单测）；③ 死导入清理（lucide 36 项/pm-state/pm-model/BLOCK_COMMANDS/BLOCK_BACKGROUND_TYPES）。**刻意不拆**：块拖拽与框选的 pointer 状态机（与组件 setState 深耦合，拆出只增透传不降审查成本）——理由记入 block-interaction.ts 头注释。**行为等价验证**：基线拆分前编辑器套件 30 文件/210 用例全绿（含 combination-regression 序列化矩阵——拖动/多选/列表/表格/列布局的 getJSON + 重解析往返断言）；拆分后 31 文件/217 用例全绿（+keydown 工厂 7 断言：IME 组合态放行/标题行尾回车/嵌套块忽略 ⌘//⌘F 搜索/无关键放行）；typecheck/lint 零警告；生产构建 ✓；mock E2E smoke 5/5（含笔记保存导航往返）。editor 实例与扩展引用未动（useEditor deps 与 editor-extensions.ts 装配不变） | 真实协作 E2E 由 CI collab-e2e job 覆盖回归 |
| B05 | 协作适配职责拆分 | M | 完成 | A05/B04 | engineering-agent | 本 PR | **纯移动拆分 + 可单测工厂**：① 新 `components/editor/collab-seeding.ts`——播种租约协议状态机整体迁出 tiptap-editor（seed-req 申请守卫 / grant 只写一次：isEmpty 守卫 + emitUpdate=false / seed-wait 2.5s×3 封顶重问 / seed-deny 立即解锁 + 12s 观察窗 / 内容到达解锁 / detach 解绑清理复位），依赖用最小结构接口（CollabSeedEditorLike/CollabSeedProviderLike），不 import TipTap/Hocuspocus 类型，真实实例在组件装配处结构兼容；UI 关切（blocked→setEditable 门控、deny toast 文案）留在 tiptap-editor 经 onBlockedChange/onDenyTimeout 回调上抛。② `lib/collab/transaction-source.ts` 增 `resolveTransactionSource` 纯函数——onUpdate 内联来源判定逐字迁移（y-sync$ → remote-sync 优先级最高、transactionSource meta 透传、兜底 user），`TransactionMetaReader` 最小接口可测。**刻意不拆**：UniqueID 初始回填 effect（doc 遍历 + 块 id 域知识，A04 已明确编辑器手动接管）、use-note-collab 会话层（A05 已自成模块）——理由记入 collab-seeding.ts 头注释。**验收对应**：并发冷启动只播种一次 = grant isEmpty 守卫（客户端）+ 服务端租约仲裁（seed-lease.ts 未动）；断线恢复不重复 = synced 再入时房间非空不发 seed-req；远端变更不触发用户任务副作用 = resolveTransactionSource 分类 + 保存会话既有合同（remote-sync 不标脏不排队不激活，本卡未动）。**验证**：新单测 16 条（播种状态机 11：冷启动申请/空笔记不阻塞/非空不重播/grant 单次/wait 封顶共 4 问/deny 解锁+观察窗仅一次/内容到达免报/detach 清理/销毁守卫/坏 payload 忽略；来源解析 5）；Vitest 153 文件 / 1,163 用例；typecheck/lint 零警告；web 生产构建 ✓（真实后端与 mock 两口径）；collab-server build ✓ + 24/24；本地完整栈协作 E2E **14/14**（collab/anon-collab/synced-block 7/revocation 3/backlinks，COLLAB_E2E=1 REAL_DB_E2E=1 SUPABASE_SERVICE_ROLE_KEY 实跑，2026-09-14）+ mock smoke 5/5（非协作编辑器保存导航往返，覆盖来源解析非协作分支） | 真实生产部署协作行为沿用 A03/D01 未验证边界（本地完整栈 + CI 已覆盖验收语义） |
| B06 | 有证据的本地存储优化 | L | 取消（证据充分） | 仅 B02 证实瓶颈后 | — | B02 PR | B02 实测（3 轮中位数，docs/handoff/b02-measurement.md §4/§5）：草稿峰值 92.5KB（5 万字×30 图）/267KB（列表1k），序列化+存储 <1ms/次，打字零长任务，重开 ≤342ms——localStorage 同步写无瓶颈证据；与 R12 两轮独立测量一致。实测瓶颈在编辑器大列表渲染（1 万条冷开 71.6s），属编辑器域非存储域 | — |
| B07 | 附件可携带备份 | L | 进行中（B07-2 已交付） | B01（先交设计） | engineering-agent | B07-1 #277；B07-2 本 PR | 设计定稿：`docs/attachment-portable-backup-design.md`——四类资源形态核实（A Storage 附件本体缺口 / B 远程抓取图 / C base64 自包含 / D 失效外链；Storage 路径含 userId 前缀故恢复必须重映射 URL）；包格式 = zip(STORE) + manifest（files+url_map+sha256+external_urls 如实声明）+ 成对 JSON（v5 schema 不动）。**§7 四项用户已确认按文档默认值（2026-09-15）：zip+STORE / 外链不打包 / 500MB·5000 文件 / 脚本先行 UI 随实现。B07-2 已交付（本 PR，导出侧）**：核心库 `lib/backup/attachment-package.ts`——白名单字段扫描分类（notes/note_versions/suggestions 正文 JSON + reading 正文/封面 HTML + synced_blocks + tasks.description + task_attachments 元数据行；reading_items.url 来源字段不进 external_urls；A 去重、B/D 外链清单截断位如实、C base64 计数）+ 路径合同前置保证（字符白名单 + 拒 `..`/`.` 段，B07-3 解包合同的导出侧源头）+ fflate ZipPassThrough 流式 STORE 打包（sink/下载可注入，单测免真实 Storage）+ AbortSignal 取消 + 500MB/5000 文件护栏明确报错不静默截断 + manifest 后置落包（sha256/字节数下载完才可知）；CLI `scripts/attachment-backup-export.mts` 成对产出 v5 JSON + zip（用户会话 RLS 下载、SIGINT 取消丢弃半成品）。**验证**：单测 10 条（分类/去重/白名单报错/zip 轮转 STORE method=0/sha256 复核/文件数与字节护栏/预中止与下载中中止/下载失败即弃）；本地真实栈冒烟 17 断言全 PASS（真用户 Storage 上传 → CLI 导出 → 解包逐字节比对源对象 + manifest 合同 + v5 JSON inspect 通过）；typecheck/lint 零警告；Vitest 158 文件/1,211 用例 | B07-3 恢复侧（安全解包/Storage 重放/URL 重映射/missing 清单）与 B07-4 UI+端到端演练未开工；导出侧 UI 入口未接（按 §7-d 默认脚本先行）；真实生产 Storage 大对象/超大包场景未测（护栏为单测级验证） |

### C 波次：界面可持续修改与多端闭环

| ID | 卡 | 规模 | 状态 | 依赖 | 执行者 | PR/commit | 关键证据 | 未验证项 |
|---|---|---|---|---|---|---|---|---|
| C01 | 可持续改版界面地图 | S | 完成 | A01 | engineering-agent | 本 PR；键位校验 本 PR | 交付 `docs/ui-change-guide.md`：L0-L4 分层地图（token/主题色 inline 覆盖机制/18 件原语/壳组装顺序）、「改哪里影响哪里」速查表（13 类常见改动 × 牵动面，含导航四处入口同步义务）、功能入口追踪表（16 功能 × 4 渠道 + 条件入口）、一页改版模板 + 维护规则。事实经只读盘点核实（file:line 证据）：无第二套 Button/Dialog、单断点体系（767px/md）、图谱复用全局 token。**键位一致性校验（后续候选落地）**：实施时发现真实漂移——`g m`（速记）已注册但帮助清单与 gotoMode 提示串均漏同步（提示串连 `m` 都没有）；修法 = `GOTO_ROUTES` 单源化（g 键位注册/帮助条目/提示串三方派生，渲染结果仅按注册表补上 g m 一行与提示 m 一字符，零其他行为变化）+ `global-hotkeys.test.ts` 8 断言钉住「注册↔帮助↔提示」一致（死文档/遗漏/重复键位/路径合法性/手工条目在位）；ui-change-guide 行号引用同步更新 | 键位清单与 NAV_ITEMS 的自动化校验已覆盖**全局 g 层**；页面级键位（n、/、v、m、x 等 5 页面组件内注册）与 PAGE_SHORTCUTS 清单仅做格式卫生检查，端到端核对仍未自动化（后续候选） |
| C02 | 键盘与可访问性改进 | M | 基本完成（自动扫描类六轮收官） | C01 | engineering-agent | 第一轮 #281；第二轮 #282；第三轮 #283；第四轮 #284；第五轮 #285；第六轮 本 PR | **第一轮：读屏名称类**（#281）——审计工具 `scripts/a11y-scan.mjs`（axe 注入式，9 路由 + 3 交互态 + 640px 200% 代理）；24 处无名称控件补 aria-label（21 SelectTrigger + 经验菜单 + 笔记排序按钮，后者顺带修键盘聚焦 opacity-0 不可见）；回归门 `e2e/a11y-button-names.spec.ts` 进 CI。**第二轮：标题层级类**（本 PR）——h1 后条目标题跳级到 h3（heading-order 6 节点）+ 笔记编辑页无任何 h1（page-has-heading-one 1）：reading-card / favorites / empty-state（10 页共用空态，核实无一处嵌于 CardTitle 内，全局换安全）三处条目标题 h3→h2（类名不变视觉零改动）；lesson-card 的 CardTitle(固定 h3) 换裸 h2（类名为 twMerge 后视觉等价集）；notes/[id] 补 sr-only h1（标题在编辑器内联渲染，视觉不重复）。复扫 heading-order + page-has-heading-one 全站归零；回归 spec 增第二用例。**第三轮：编辑器壳杂项**（本 PR）——① note-page-visuals 封面上传 sr-only file input 补 aria-label「上传封面图片」（axe label critical）；② 编辑器壳 aria-allowed-attr critical：tippy（BubbleMenu 底层）interactive 模式给无角色参考 div 挂 aria-expanded，tippy 6 无 a11y 开关、TipTap v2 不透传 reference 替换——编辑器内 MutationObserver 定点摘除 shell 直接子 div 的该属性（合法的 Radix 触发器状态不受影响），复扫归零；回归 spec 编辑器页加选中文本态断言 aria-allowed-attr + label。**第四轮：对比度类**（#284）——侧栏三处次级列表选中态（最近/标签/笔记树）+ 任务工作台侧栏选中态 `bg-primary/10 text-primary`→中性 `bg-accent text-accent-foreground`（orange 叠侧栏底 4.47:1 差线不过且品牌色敏感 pink 光模式白底 3.7:1；对齐 D02 §4「选中底色一律中性」，对 5 品牌×明暗构造性免疫）；标题区添加图标/封面/评论按钮 muted-foreground /0.85→全值（focus-within 键盘可见态 3.89→5.3:1）；扫描脚本补 /notes/[id] 默认态（hover-only 盲区曾漏扫）+ 可选品牌档 A11Y_SCAN_BRANDS；复扫 color-contrast 全站归零；**品牌档量化残余：pink 6 节点（bg-primary/10 text-primary 徽章、memos 标签链等）/ blue 0**——品牌敏感主色文本留作后续系统修（需按品牌派生安全文本 token）。**第五轮：嵌套交互类**（#285）——/tasks 任务行 role=button+tabIndex 内嵌 4 原生按钮（nested-interactive serious，读屏吞内层控件）：行改非交互容器（保留整行点击/长按/拖拽），标题改原生按钮作键盘入口（点击/Enter 冒泡走行 onClick 开详情），x 快捷键经行 keydown 保留；回归 spec 用例 3 追加 nested-interactive；复扫 nested-interactive 归零——**axe 可扫类全站归零（含 200% 口径）**。**第六轮：品牌安全文本 token 系统修**（本 PR）——全品牌复扫实测 pink 之外 **green 同病**（text/白 3.07、白fg/primary 2.94，比 pink 更差）、blue/purple 光模式通过；修法三层：① 新增 `--primary-text` 品牌安全文本 token（pink 340/75/40、green 145/65/28、purple 270/70/52，orange/blue 原值）+ tailwind `textColor.primary` 覆盖使全站 84 处 `text-primary` 一次性解析到安全色（bg/border/ring 仍取品牌原色）；踩坑：字符串覆盖会把 `text-primary-foreground` 从产物 CSS 整个顶掉，必须对象形式保留 foreground 子键；② pink/green 光模式 `primaryFg` 降亮度为品牌近黑（白字粉底 4.03→近黑 4.70、绿底 2.94→5.43，与暗色模式既有「深字亮底」做法一致）；③ purple 暗色 `primaryFgDark` 3.94→纯黑 4.64、blue/pink 暗色 primaryTextDark 提亮（暗 tint 底 4.47/4.27→4.57/4.99）。globals.css 9 处文本色（历史面板激活项/徽章、编辑器与阅读器链接、数据库链接/日历项/时间线）同步切安全 token，边框/accent-color 等非文本用途保留原色。**契约钉子**：新增 `hooks/use-theme-color.test.ts` 21 断言（5 品牌 × 光/暗 × 文本底/fg 底全部 ≥4.5:1，背景镜像 globals.css token）。**验证**：a11y-scan 默认档 + pink/blue/green/purple 四品牌档 + 200% 口径全部 0 违规（本地 mock 栈实跑）；typecheck 零错误、Vitest 156 文件/1,193 用例、lint 零警告、生产构建 ✓、mock E2E 8/8（smoke 5 + a11y 3） | 键盘全程人工路径、焦点回归、真实读屏、真浏览器 200% 缩放人工核验未做（真机项）；**已知边界**：pink/purple 光模式 `hover:bg-primary/90` 瞬态白字 ~4.46/4.41:1（背景亮度死区数学无解，axe 不可扫瞬态，契约按 resting 态钉住，记录于 use-theme-color.ts pink 段注释）；green 活跃 tab `border-primary` 非文本对比 2.85:1（WCAG 1.4.11 略欠，axe color-contrast 不扫边框，留待人工审计域）
| C03 | 手机真机工作流 | M | 候选 | C01 + 真机 | — | — | 手机 **Web 布局**已完成（PR #257 底部五模块）；**原生 App 发布**是另一回事（见下方分流） | 中文输入法/软键盘/安全区/横竖屏全部待真机 |
| C04 | macOS 刘海与多屏矩阵 | M | 候选 | A01 + 真机 | — | — | `docs/macos-notch-compatibility-review-2026-09-06.md` + 后续修复（`07b51d6` 等）；不能重复修已合并项 | 混合缩放/热插拔/睡眠唤醒待矩阵 |
| C05 | 通知端到端闭环 | L | S1–S3 完成 + S4 本地段交付；真实 Push 归 staging | A02/A03 | engineering-agent | S1 #286；S2 #290；S3 #291；S4 本地 本 PR | 盘点设计文档 `docs/handoff/c05-notification-design.md`（四条投递路径职责/去重键/生命周期；双响防线三道锁完好 + 登记两个残余同刻双响面 D1 web 本地 vs push、D2 tauri 页面排程 vs 轮询；DST 决策项 R1=033 重复任务绝对 interval 推进跨夏令时墙钟漂移 1h）。**S1**（#286）：web 本地通知点击跳转闭环 + cron 重试决策抽 push-delivery 纯函数 + 10 单测。**S2**（#290，用户已确认按建议 (a) 默认语义实现——同一任务「到期」与「开始/结束」±15min 窗口内视为同一事件只响一条，窗口值 PR 可调）：**D1**=use-notifications 排程前经 RLS 拉 task_reminders 配置集（reminderFireAt 计算绝对时刻），due 变体触发时刻与配置行重叠 → 跳过本地报（服务端 Push 多端一致优先）；拉取失败/空集降级为不抑制，不引入新失败面，mock 空表行为不变。**D2**=tauri 轮询发通知前查 organize:notified-due 本地已通知键，同任务 due 时刻 ±15min 内 → 抑制轮询通知（键被消费防重复判）。纯函数 filterShadowedReminders / isSuppressedByLocalDue / dueMomentsByTaskFromNotifiedKeys + NOTIFIED_DUE_STORAGE_KEY 统一导出（use-notifications 写、poller 读同源）。**验证**：Vitest +6（窗口边界 ≤、配置行窗口外/空集不剔除、不合法键忽略等）；typecheck/lint 零警告；生产构建 ✓。**S3**（本 PR，R1 默认墙钟语义用户已确认）：迁移 080 新增纯函数 `advance_recurring_wall_clock`（tasks.timezone AT TIME ZONE 墙钟日历推进，monthly/yearly 夹取由 naive 运算完成，tz null/非法返回 null 供回退），complete_recurring_task 改为「合法 timezone 走墙钟 → 跨 DST 本地钟点不漂移；否则回退 033 绝对推进」——存量无时区任务行为不变，R2 一并定案（任务按创建时区解释，不随查看设备变）。**验证**：pgTAP 080 共 17 断言（春/秋令时墙钟保持、weekly、月末 1/31→2/28、闰日 2/29→2/28、回退信号三类、RPC 端到端生成+幂等+越权负例），全套 34 文件/911 断言 PASS（隔离本地栈）；设计文档 §3.2 已更新定案记录。**S4 本地段**（本 PR）：按设计文档 §3.1 交付 cron 路由投递分支 stub 单测——鉴权 401、未配置 503、claim 失败 500、成功置 sent+sent_at（payload 含 /tasks/{taskId} 跳转 URL）、410 永久失败停订（web_push_subscriptions.disabled_at + next_attempt_at=null）、500 瞬时失败按 nextRetryDelayMinutes(2^n) 指数退避，共 6 断言（stub web-push 纯 Node 层，不冒充推送服务）。**浏览器订阅自动化实测结论**（三种浏览器配置 × 三种授权方式，含有头真实 Chrome + Notification.permission=granted，pushManager.subscribe 全部 `AbortError: Registration failed - permission denied`）：push 服务客户端注册层在自动化/临时 profile 环境不可用，无法本地自动化真实订阅→真实推送——证据与结论已记入设计文档 §3.1，真实 Push 端到端维持 staging/真设备归口，不降级为合成事件冒充 | S4 剩余：真实订阅→真实推送→离线补投 + lastSentAt 告警接线（staging：VAPID/公网 HTTPS/真设备）；S5 capacitor 随移动壳；D1 收敛假设生产 cron 已配置（TASK_REMINDER_BASE_URL 未配时服务端不投递、本地又被抑制 → 该场景用户少一条提醒，接受并记录）；D2 残余极窄竞态（页面已排程未触发的 due 超时先被轮询抢报）记录于 reminder-poller.tsx 头注释；tauri v2 通知无点击回调为平台限制 |
| C06 | 功能入口整合提案 | S | 候选 | C01/C03 | — | — | 候选：任务/经验复盘、笔记/图谱、速记/快速记录 | 产品方向待用户定 |

### D 波次：可控部署与多端内部发布（外部凭据依赖）

| ID | 卡 | 规模 | 状态 | 依赖 | 执行者 | PR/commit | 关键证据 | 未验证项 |
|---|---|---|---|---|---|---|---|---|
| D01 | 部署差距清单与 staging 恢复 | M | 阻塞（外部凭据） | A02–A05/B01 + 云凭据 | — | — | 有效阻塞：云库停在 062（BLOCKED.md P2-03 复核，2026-08-31，时值 master 067，现已 075 更落后）；本机无 Supabase access token / Vercel 登录态 | 实际部署 commit/域名归属需重新只读核实，不信任旧版本号 |
| D02 | 桌面受控内部发布 | L | 候选 | D01/C04 | — | — | `frontendDist` 曾指向被第三方占用的 `organize-web.vercel.app`（P4-01 警告），现归属需重验 | 安装→更新→深链全链路 |
| D03 | Android 内测版 | L | 候选 | D01/C03 | — | — | `mobile/` Capacitor 骨架未完整实现（AGENTS.md） | 真机核心路径 |
| D04 | iOS TestFlight 准备 | L | 候选 | D03 | — | — | 开发者账号/证书为外部依赖 | — |
| D05 | 正式发布与日常运维 | M | 候选 | D01 | — | — | — | — |

### E 波次：按使用反馈候选（须用户选定方向后才开工）

E01 导入迁移工作台、E02 阅读→行动闭环、E03 每日回顾、E04 AI 体验整理、E05 插件兼容合同、E06 数据模型长期演进——全部**候选**，无执行者。

## 易混淆项分流（A01 验收要求）

以下三组最容易把「部分完成」误读为「全部完成」，任何 Agent 领卡前先对照：

1. **R10a 完成 ≠ R10b 完成**：反链已有过渡方案（迁移 `074_note_backlinks_rpc.sql` RPC 文本匹配 + `lib/notes/backlinks.ts` 客户端聚合 ≤5,000 条）；精确关系索引（服务端维护 canonical 关系、授权可见性、稳定分页）是 B03，**未开工**。
2. **R12 已测 ≠ 性能已全面测量**：`docs/handoff/r12-measurement.md` 只有无图片文字样本；savePosts 计数与 draftSize 仪表当时**未捕获到数据**，10/30 张图片、复杂表格、大列表、图谱样本、移动 Safari 均未测——补齐是 B02。
3. **手机 Web 布局完成 ≠ 原生 App 发布完成**：PR #257 完成的是手机浏览器/PWA 布局（底部五模块+顶部工具）；原生 Android（D03）/iOS（D04）连内测构建都未开始，真机工作流（C03）未验收。

## 已完成能力基线（勿再从零开发）

| 能力 | 实现证据 |
|---|---|
| 稍后读统一收集（含失败降级） | `apps/web/lib/reading/collect.ts`（P1-01，PR #180）；五入口均为薄壳 |
| Markdown 导出与本地快照 | `lib/export/tiptap-to-md.ts`、`note-export.ts`（R01/R02） |
| 笔记保存会话与草稿错误可见 | `lib/notes/note-save-session.ts`、`hooks/use-note-session.ts`（R03/R07） |
| 同步块 revision 与冲突处理 | 迁移 073、`components/editor/extensions/synced-block.tsx`（R04/R05） |
| 反链过渡方案（R10a） | 迁移 074、`lib/notes/backlinks.ts` |
| 速记转笔记事务与关联 | 迁移 075 `memo_notes`（R11）；备份 v5 |
| 界面主题/主页面重排/辅助面板 | `docs/handoff/execution-log.md` D00–D06；移动导航 PR #257 |
| 登录与匿名笔记协作 | collab-server（ADR 0003）、`use-note-collab.ts`、071/072 |
| macOS 壳/刘海激发器/Windows 发布管线 | `desktop/`、`docs/notch-trigger-plan.md`、desktop workflows（W1–W6） |
| AI 安全边界 | `lib/ai/safe-request.ts`、`user_ai_settings` 走 `/api/ai/settings`（P0-03） |

## 历史 BLOCKED 三分类

`BLOCKED.md` 保留全部声明正文；此处只维护分流感见（2026-09-11）：

- **有效阻塞**（仍真实挡着，需外部条件或专门卡）：
  1. 云部署落后：staging 云库停在 062（master 已 075）+ 无云凭据 → D01 领卡时先重新只读核实（旧版本号只作参考）。
  2. ~~协作连接内 token 刷新~~（**2026-09-12 A05 核实解除**：provider 4.6 `token` 支持函数形式每次重连重新求值，server `requestToken`/`onTokenSync` 可重验存量连接——详见 `docs/collab-session-refresh-design.md` §1；BLOCKED.md 原声明保留为历史）。
  3. 真机验收（C03/C04/D02–D04）：需硬件与人工，验收 Agent 出步骤包。
- **已解除**（历史记录，现状已恢复，勿再当阻塞引用）：
  1. Docker 拉取通道 hang（069 卡记录已恢复，`supabase start -x supavisor,imgproxy` 正常）。
  2. Rust 工具链缺失（P4-01 已经 rustup 装好；注意 cargo 不在默认 shell PATH，见交接）。
  3. 本机 postgres 角色权限漂移致 059 恒红（CI 全新库绿，属本机环境非代码缺陷）。
- **合同说明**（不是阻塞；违反会重开坑，改动相关域前先读 `BLOCKED.md` 对应声明）：
  匿名无归属/匿名不可改任务勾选/限流双 backend（多实例须 postgres，A06）/撤销下次连接生效/播种前空文档不落库/SMTP 未配不发信（Track A/B）；task_item_refs 复合 FK 移交语义与 PG 递归 CTE 限制（069/070）；评论作者列租户取舍（068）；blob 与快照分工、播种租约协议（P5-03 生产化）；备份排除清单是声明不是枚举；`last_edit_by` 四处口径（066）；051 复选框精确语义（065）；Windows 发布三条件硬门/私钥永不入库/双响防线（W1–W6）。

## 账本维护规则

1. 一张卡一个 PR；PR 内同步更新本表对应行（状态、PR/commit、证据、未验证项）。
2. 状态只能取：候选、就绪、进行中、待验证、阻塞、完成、取消；因不值得实施而取消也是有效结果（如 B06 无瓶颈证据时）。
3. 完成定义与验证层次见计划 §6；不得靠 skip、删测试、放宽断言、空实现或假成功交付。
4. 同一 checkout 同一时间只有一个写入 Agent；只读审查/设计/验收 Agent 的产出放仓库外（`/tmp/...`），由工程 Agent 核实后收编进本表。
5. 全局基线数字只在实测后更新（注明命令与 commit），不照抄历史。

## 运维注记：collab-e2e 偶发失败的重跑惯例（2026-09-13，B03-4/B07-1 实测）

B03-4 与 B07-1 两个 PR 的 collab-e2e job 各出现一次**不同用例**的超时失败（collab.spec 一次、collab-revocation 一次），重跑 job 即全绿；B03-4 首跑失败经 master 状态复跑确认为存量环境时序、与本卡无关。**惯例：collab-e2e 单用例 `locator.click: Test ended` / 15s 期望超时类失败，先重跑一次 failed job 再排查；同一用例连续两轮失败才按真实回归排查。**（已知诱因：B03-4 起套件 +1 spec，job 时长 +~1min，暴露窗口略增。）

## 运维注记：分支保护 required checks 的写法（2026-09-12，A04 合并时实测）

master 分支保护的 required status checks 必须写 **check run 裸名**（`verify` / `e2e-test` / `sw-e2e` / `collab-e2e` / `db-test`），不要写 `CI / verify` 这类「工作流 / 任务」串：经 API 写入的带前缀串不会绑定到 Actions check run（`app_id: null`），五个检查全绿也永远报 `5 of 5 required status checks are expected`、合并被 405 拒绝（A03 写入的配置即此坑，PR #263 首次触发；改裸名后立即绑定 `app_id: 15368` 并放行）。改保护配置：`gh api -X PUT repos/joyworkon/Organize/branches/master/protection --input -`，body 为 `{"required_status_checks":{"strict":false,"contexts":["verify","e2e-test","sw-e2e","collab-e2e","db-test"]},"enforce_admins":true,"required_pull_request_reviews":null,"restrictions":null}`。
